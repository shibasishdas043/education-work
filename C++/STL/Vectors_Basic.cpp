#include <iostream>
#include<map>
#include<algorithm>
#include<bits/stdc++.h>

using namespace std;

int main()
{

    // map<string, int> age;

    // age["Alice"] = 25;
    // age["Bob1"] = 50;
    // age["Bob2"] = 70;
    // age["Bob3"] = 40;
    // age["Bob4"] = 90;

    // for (auto iterator : age)
    // {
    //     cout<<iterator.first<<" -> "<<iterator.second<<", ";
    // }

    map<string, int> data;

    string str;
    int value;

    for (auto i : data)
    {   
        if (data.size() < 3)
        {
            cout<<"Enter The Name";
            cin>>str;

            data.push(str);

            cout<<

        }
        



    }
    



































    return 0;
}
