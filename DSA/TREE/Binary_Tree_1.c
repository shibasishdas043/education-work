#include<stdio.h>
#include<stdlib.h>

struct node {
    int data;
    struct node *left;
    struct node *right;
};

struct node *create_Node(struct node *root) {   
    root = malloc(sizeof(struct node));
    root

}

int main(){

return 0;
}