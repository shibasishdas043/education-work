#include<stdio.h>
#include<stdlib.h>

struct node {
    int data;
    struct node *leftLink;
    struct node *rightLink;
};
struct node *head;

void insertAtHead(int value) {
    struct node makeNode;
    
}

int main(){

return 0;
}